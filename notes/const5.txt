Функции

function name(argumensts){
    comand;
    .....
    return argument;
}

Обращаемся к функции через ее имя и передаем аргумент.
    name(argument);

Замыкание функции это обращение функции к глобальным переменным.