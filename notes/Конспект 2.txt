Команды для взаимодействия с пользователем:
alert(""); - выводит сообщение на экран
let/const name = confirm("Сообщение с вариантами да/нет"); - выводит запрос да/нет
let/const name = promt("Вопрос", "Ответ по умолчанию"); - задает вопрос и сохраняет ответ в переменную в String
typeof("name") - узнать тип переменной


Интерпаляция (более продвинутая КОНКАТЕНАЦИЯ) используется ТОЛЬКО в `` таких кавычках(бэктики - коссые одирные ковычки)
alert(`Что-то, число, ${ранее созданая переменная}`)

ИНКРИМЕНТ name++; увеличивает на 1 и ДИКРИМЕНТ name--; уменьшает на 1
Если знак+/- стоит ПЕПЕД ++name то метод называется ПРЕФИКСНЫЙ (сначала увеличивает и затем возвращает), 
а наооборт name++ ПОСТФИКСНЫЙ (сначала возвращает значени, а затем его увеличивает)

И &&, Или ||, !name (отрицание)

Тирнарынй оператор (состоит из трех аргументов)
(name === 50) ? comand : comand;

Switch - оператор для строгой проверки условий
switch (name){
    case 56:
	comand;
	break;
    case 57:
	comand;
	break;
    case 58:
	comand;
	break;
    default:
	comand;
	break;
}